-- https://github.com/brndnmtthws/conky/blob/main/data/conky.conf
-- modified by dgkim
-- Obtain CPU Temperature from /sys/class/thermal/thermal_zone1/temp
conky.config = {
    alignment = 'bottom_right',
    background = false,
    border_width = 1,
    cpu_avg_samples = 2,
    default_color = 'cyan',
    default_outline_color = 'white',
    default_shade_color = 'white',
    double_buffer = true,
    draw_borders = false,
    draw_graph_borders = true,
    draw_outline = false,
    draw_shades = false,
    extra_newline = false,
    font = 'DejaVu Sans Mono:size=10',
    gap_x = 20,
    gap_y = 20,
    minimum_height = 5,
    minimum_width = 5,
    net_avg_samples = 2,
    no_buffers = true,
    own_window = true,
    own_window_type = 'utility',
    own_window_argb_visual = true,
    own_window_argb_value = 64,
    show_graph_range = false,
    show_graph_scale = false,
    stippled_borders = 0,
    update_interval = 1.0,
    uppercase = false,
    use_spacer = 'none',
    use_xft = true,
};

conky.text = [[
${color tomato}Uptime:$color $uptime ${color tomato}Temp:${color}${execi 1 cat /sys/class/thermal/thermal_zone1/temp | sed 's/...$//'}°C
${color tomato}CPU Usage:$color $cpu% ${cpubar 10}
${color tomato}RAM:$color $mem/$memmax - $memperc% ${membar 6}
${color tomato}Swap:$color $swap/$swapmax - $swapperc% ${swapbar 6}
${color tomato}Processes:$color $processes  ${color grey}Running:$color $running_processes
${color green}---------------------------------
${color tomato}File systems:
 / $color${fs_used /}/${fs_size /} ${fs_bar 6 /}
]];

[project]
name = "first"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "dotenv-cli>=3.4.1",
    "httpx>=0.28.1",
    "mcp[cli]>=1.6.0",
]

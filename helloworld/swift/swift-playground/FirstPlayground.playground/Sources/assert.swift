import Foundation

public func testAssert() {
    //var age: Int = 40
    //
    //assert(age > 0, "Age is under 0")
    //print(age)
    //
    //age -= 41

    //assert(age > 0, "Age is under 0")
    //precondition(age > 0, "Age must be greater than zero.")
    //print(age)
    //assertionFailure("Should not be happening")
    //fatalError("HAHAHA")
}

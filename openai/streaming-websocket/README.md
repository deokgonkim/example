# testing openai chat streaming api with websocket

## Run server

- prepare `server/.env` with `OPENAI_API_KEY`
- run server
  ```bash
  cd server
  npm run start
  ```

## Run client

```bash
cd client
npm run start
```
